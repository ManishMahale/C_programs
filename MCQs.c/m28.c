#include <stdio.h>
#include <conio.h>
void main()
{
    float a = 0.7;
    if (a < 0.7)  //a < 0.7  /here 0.7 datatype is bay default is duble   and float < duble
        printf("Stoned");//so conditi is true
    else
        printf("Avenged");
    getch();
}
/* if we write if(a < 0.7f)
then O/P = Avenged */